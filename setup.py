from distutils.core import setup
setup(
  name = 'sniffer',
  packages = ['sniffer'], # this must be the same as the name above
  version = '0.1',
  description = 'Info about incoming packet',
  author = 'Dinesh Yadav',
  author_email = 'dineshyad72@gmail.com',
  url = 'https://github.com/dineshvipe/Sniffer', # use the URL to the github repo
  download_url = "", # I'll explain this in a second
  keywords = ['testing', 'logging', 'example'], # arbitrary keywords
  classifiers = [],
)